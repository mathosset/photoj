<% wrap_layout :layout do %>
  <div class="big-spacer"></div>

  <div id="wrapper">
    <div class="container">
      <div class="row">
        <div class="col-sm-12 col-md-12">
            <article>
              <p>/// <%= current_article.data.date.to_date.strftime('%e %b %Y') %> ///</p>
              <h1><%= current_article.data.title %></h1>
              <%= yield %>
              <div class="spacer"></div>
              <% if current_page.article_previous %>
                <div id="previous-post">
                  <% link_to current_page.article_previous do %>
                    <i class="fa fa-angle-double-left"></i>
                    <%= current_page.article_previous %>
                  <% end %>
                </div>
              <% end%>
              <% if current_page.article_next %>
                <div id="next-post">
                  <% link_to current_page.article_next do %>
                    <%= current_page.article_next %>
                    <i class="fa fa-angle-double-right"></i>
                  <% end %>
                </div>
              <% end%>
              <div class="big-spacer"></div>
          </article>
        </div>
      </div>
    </div>
  </div>
<% end %>
