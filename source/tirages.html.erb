---
title: "Mathieu Thomasset - Tirages d’Art"
description: "Galerie de tirages photographiques d’art en éditions limitées, signées et numérotées – Mathieu Thomasset Photographe."
preferred_url: "http://www.mathieuthomasset.fr/tirages"
---

<div class="big-spacer"></div>

<section class="home-intro">
  <div class="container-fluid">
    <div class="row">
      <div class="col-xs-12 col-sm-10 col-md-offset-1">
        <h1 id="upper">VENTE DE TIRAGES</h1>
        <p>
          Découvrez ma galerie virtuelle de <strong>tirages photographiques</strong> disponibles en <strong>Open Edition</strong> ou en <strong>Editions Limitées</strong>, numérotées et signées. Cliquez sur une image pour découvrir ses détails, ses mises en scène et les formats disponibles.
        </p>
      </div>
    </div>
  </div>
</section>

<div class="container-fluid">
  <section class="masonry-grid tirages-grid">
  <div class="grid-sizer"></div>
  <% data.tirages.each do |t| %>
    <div class="grid-item tirage-card">
      <a href="/tirages/<%= t.id %>.html" class="tirage-link">
        <div class="tirage-square">
          <div class="tirage-frame">
            <%= image_tag "tirages/#{t.original}", class: "tirage-img", alt: "#{t.title} — #{t.year}" %>
          </div>
        </div>
      </a>
      <div class="tirage-info">
        <h3 class="tirage-title"><%= t.title %></h3>
        <% if t.default_paper %>
          <p class="tirage-paper">✔ <%= t.default_paper %></p>
        <% end %>
        <% if t.default_price %>
            <p class="tirage-price"><%= t.default_price %></p>
          <% elsif (t.prices || []).any? %>
            <p class="tirage-price"><%= t.prices.first[:price] %></p>
          <% end %>
      </div>
    </div>
    <% end %>
  </section>
</div>

<div class="spacer"></div>
