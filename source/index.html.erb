---
title: Photographe Portrait Reportage Montaigu en Vendée
description: Photographe basé en Vendée Grand Ouest - Reportage, Portrait, Corporate pour la Presse, les Entreprises et les particuliers - Membre du studio Hans Lucas
preferred_url: http://www.mathieuthomasset.fr
---

<header id="myCarousel" class="carousel slide">
    <!-- Indicators -->
    <!--
    <ol class="carousel-indicators">
      <% (data.home || []).each_with_index do |y, index| %>
        <li data-target="#myCarousel" data-slide-to="<%= index %>" class="<%= 'active' if index == 0 %>"></li>
      <% end %>
    </ol>
    -->
    <!-- Wrapper for Slides -->
    <div class="carousel-inner">
      <% (data.home || []).each_with_index do |y, index| %>
        <div class="item <%= 'active' if index == 0 %>">
          <div class="fill" style="background-image:url('../images/<%= y.picture %>');"></div>
          <div class="carousel-caption">
            <h1><%= y.title.upcase %></h1>
            <h2><%= y.subtitle %></h2>
            <div class="spacer"></div>
            <a href="<%= y.page %>" class="btn-medium"><%= y.category %></a>
          </div>
        </div>
      <% end %>
    </div>
    <!-- Controls -->
    <!--
    <a class="left carousel-control" href="#myCarousel" data-slide="prev">
        <span class="icon-prev"></span>
    </a>
    <a class="right carousel-control" href="#myCarousel" data-slide="next">
        <span class="icon-next" aria-hidden="true"></span>
    </a>
    -->
</header>

<div class="container">
  <div class="mid-spacer"></div>
  <div class="row">
    <% (data.homecards || []).each_with_index do |y, index| %>
      <div class="col-xs-12 col-sm-4">
        <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.15), rgba(0,0,0,0.1)), url('images/<%= y.picture %>');">
          <div class="card-category"><%= y.category.upcase %></div>
          <div class="card-description">
            <h2><%= y.title.upcase %></h2>
            <p><%= y.caption %></p>
          </div>
          <a class="card-link" href="<%= y.page %>" ></a>
        </div>
        <div class="spacer"></div>
      </div>
    <% end %>
  </div>
  <div class="mid-spacer"></div>
  <div class="row">
    <div class="col-sm-12">
      <p><%= partial 'layouts/bofpublications' %></p>
    </div>
  </div>
</div>
